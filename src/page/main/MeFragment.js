import React, {Component} from 'react';
import {View, TouchableOpacity, Text, NativeModules} from 'react-native';

export default class MeFragment extends Component {
  render() {
    return <View style={{width: 100, height: 60, backgroundColor: 'yellow'}}/>
  }
}